/**
 * Dependencies.
 */
import { Base } from './base';

/**
 * Scene class.
 */
export class Scene extends Base {
	constructor(game) {
		super();
		// Keep reference to the game object.
		this.game = game;
	}
}
